# Ignore everything by default
*

# But allow all directories so we can add exceptions later
!*/

# Always include the .gitignore and .uproject file
!.gitignore
!/*.uproject

# Ignore most of the Content folder (besides exceptions above)
# This line can be removed if you want more folders tracked manually
/Content/*

# Allow source code and key content folders
!/Source/
!/Config/
!/Content/Blueprint/**
!/Content/FirstPerson/**

# Build, intermediate, cache, and saved folders – ignore them
/Binaries/
/DerivedDataCache/
/Intermediate/
/Saved/

# Ignore IDE/editor settings and build metadata
.vscode/
.vs/
*.VC.db
*.opensdf
*.opendb
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace
